import Icon from './Icon.jsx'

// Exercise media is intentionally omitted: the upstream pictures and animations are not
// licensed for redistribution. Keep this component so workout layouts remain compatible.
export default function Media() { return null }

export function Thumb() { return <div className="thumb thumb-x"><Icon name="dumbbell" /></div> }
